# Chat com SignalR

Para executar, no terminal, acesse o diretório do projeto e execute o comando ```dotnet run```.

Abra duas abas de um navegador e acesse o endereço ```http://localhost:5000``` em ambas.
